import { Routes } from '@angular/router';
import { TimekeepingComponent } from './dashboard/timekeeping/timekeeping.component';

export const routes: Routes = [
  { path: '', component: TimekeepingComponent }
];
